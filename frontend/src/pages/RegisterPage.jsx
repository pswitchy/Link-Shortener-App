import React from 'react';
import RegisterForm from '../components/Auth/RegisterForm'; // We will create this

const RegisterPage = () => {
  return (
      <RegisterForm />
  );
};

export default RegisterPage;