import React from 'react';
import LoginForm from '../components/Auth/LoginForm';

const LoginPage = () => {
  // This page might not need a separate Navbar or could have a minimal one
  return (
      <LoginForm />
  );
};

export default LoginPage;